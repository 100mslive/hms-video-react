import { UseStore } from 'zustand';
import { HMSStore } from './schema';

// TODO: copy over the interface functions we require from zustand's UseStore
export interface IHMSStore <T extends UseStore<HMSStore>>{
}